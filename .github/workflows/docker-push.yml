name: Docker Push

on:
  push:
    branches:
      - main
      - master

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build & Push to Docker Hub
        uses: opspresso/action-docker@master
        with:
          args: --docker
        env:
          USERNAME: komoliddin
          PASSWORD: Gx7!dYp@92#fLm$Q
          REPO: komoliddin/js-docker-k
          TAG_NAME: v0.0.1
          BUILDX: "true"
