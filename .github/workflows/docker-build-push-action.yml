name: Docker Push

on:
  push:
    branches:
      - main
      - master

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build & Push to Docker Hub
        uses: mr-smithers-excellent/docker-build-push@v6
        with:
          image: komoliddin/js-docker-k
          registry: docker.io
          username: komoliddin
          password: Gx7!dYp@92#fLm$Q
